{
  "mcpServers": {
    "mcp-plugin-template": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/src/index.js"
      ],
      "env": {
        "MCP_PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}",
        "MCP_SERVER_NAME": "mcp-plugin-template"
      }
    }
  }
}
